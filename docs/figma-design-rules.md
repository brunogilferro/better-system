# FIGMA DESIGN RULES

> **Project-specific visual identity.**
> This file is generated by `create-better-system` during project setup.
> Do NOT copy values from other projects — each project defines its own tokens here.

---

## Typography

| Role     | Font             | CSS Variable     |
|----------|------------------|------------------|
| Headings | __HEADING_FONT__ | `--font-heading` |
| Body     | __BODY_FONT__    | `--font-body`    |

Font weights: Regular → 400 / Medium → 500 / Bold → 700

---

## Theme Mode

`__THEME_MODE__`

---

## Accent Colors (shared across themes)

| Token                    | Value                    | Usage                       |
|--------------------------|--------------------------|-----------------------------|
| `--accent-primary`       | __ACCENT_PRIMARY__       | Main brand accent           |
| `--accent-primary-hover` | __ACCENT_PRIMARY_HOVER__ | Hover state                 |
| `--accent-secondary`     | __ACCENT_SECONDARY__     | Secondary accent/highlights |

---

__COLORS_SECTION__

---

## CSS Variables

```css
__CSS_BLOCK__
```

---

## Rules for AI

When generating UI for this project:

- Use tokens from this file — never raw hex values
- Headings: `font-heading` / `--font-heading`
- Body text: `font-body` / `--font-body`
- Backgrounds: `--bg-primary`, `--bg-secondary`, `--bg-surface`
- Text: `--text-primary`, `--text-secondary`
- Accents: `--accent-primary`, `--accent-secondary`
- Theme mode: __THEME_MODE__
